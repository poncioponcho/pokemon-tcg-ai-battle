# 8/15 会师判读包（20:00 唯一执行件）

> **本件 = 2026-08-15 20:00 会师 session 的唯一执行件**（提醒 `automation_dd5afa22` 拉起的 fresh session 先读本件）。
> `experiments/pilot_investigation/convergence_cheatsheet.md` 降为 **A 轨存档**——仅其 ⑤（命令节）⑥（背景节）仍有效，本件引用不重述数字，防第三口径源。
> 铁律：截止 8/17 07:59 前一切分数**瞬读不记账**（ledger #98）；ledger **#135 为收官预留**勿占用；收官记账只走 `reports/2026-08-17_收官checklist.md`（8/15 不在议程，勿误触）。
> A 轨已 CLOSED（08-13 N1/K0=KILL，2×2 落格无产物）。08-15 的首要议程已经
> 改为：**先核对当前活跃两件和最新计分件，再判读；不得按旧 ref 自动归档/翻牌。**

## 2026-08-14 11:05 执行覆盖（以本节为准）

旧版“WR ≤ 0.62 即归档、今天不发射”的收兵规则已被用户的截止约束覆盖。另有一项
更关键的机制纠错：**团队榜不取 `max(last-2)`。** 08-14 02:58 UTC 下载的官方完整
leaderboard 中，本队分数 `600.1`、`LastSubmissionDate=02:55:41`，精确对应最新 Router；
当时前一件 Grim 的提交页分数为 `709.1`，却没有保护团队榜。`team-submissions`
也只返回最近两件，说明 last-2 是继续匹配的活跃集，不是榜分地板。

- **行动层：最新件制。** 每次有效提交都会改写团队榜当前关联件；不能再声称“较强旧件
  留在 last-2 就能托底”。每天默认只发 **1 个主候选**，第 2 发仅用于首发 ERROR 或
  证据明确的恢复，不为凑额度连续覆盖。
- **认知层：阈值制。** 只有 `WR > 0.62` 才把“存在可识别正效应”写入结论；
  `0.44–0.62` 记为“功效不足/未定”，不把噪声写成证伪。
- **发射纪律：** 每个自然日必须先完成至少一个真实 Kaggle 提交，再写日报；优先在
  当天早段发射，让最新件有最大沉淀时间。提交前必须同时打印 `team-submissions` 和
  完整 leaderboard 本队行，记录“将被最新件覆盖的当前榜分”。
  新候选以 artifact SHA、Kaggle ref、状态和 live 分数为交付物，不以 NO-GO 报告
  代替提交。
- **当前活跃集（08-14 11:05 CST）：** `55496363` = retreat 精确恢复件（最新，
  `COMPLETE`，首分 600.0，2 局 1-1）；`55496233` = Visible Router V3（次新，
  `COMPLETE`，首分 600.0，4 局 3-1）。旧 `55468450` 的 700.3 只保留为同代码历史
  live 先验，已出活跃集，不会继续保护榜分。Grim `55495955` 也已被恢复件顶出。
- 新 ref 的 episode 读取使用参数化工具，例如：
  `python3 experiments/live_episodes_probe.py --ref 55496363 --out experiments/runs/live_episodes_retreat_recovery.json`。
  工具默认 ref 已改为 `55496363`；每次新发后仍应显式传 ref，禁止依赖陈旧默认值。

### 8/15 发射 baseline（用户最终裁决）

唯一 baseline 是当前最强真实提交——纯规则 retreat 精确件：ref `55496363`，
main SHA `411d9dff4c146e3bf5b8cbbb935f6c53c84742d670d41930d8315926a61ba480`，deck SHA
`2a541d7bf3d9e6b36037123f53f4dfef6348223f79fd27095dafc602a5357c19`。所有 Router、
Grim、Search、Control、belief 版本都只是 challenger；必须在官方同引擎、交替座位的
直接 H2H 中对该 baseline 给出足够证据，才可成为当天最新提交。若截至发射窗口没有
challenger 明确胜出，直接重交 `submission_baseline/submission.tar.gz` 满足当日提交，
不得用 config A 或公共历史分替代 baseline，也不得为了探索覆盖榜件。

选择依据已冻结：config A `55451759` 的历史最高 859.1 是前 4 局 4-0 尖峰，最终
24-30；两次同构 config A 合计 50-58。retreat 原件 27-21，且两个座位分层均优于
config A 合并约 9-11pp。差异尚不显著（单侧 Fisher p=0.165），因此行动层以
retreat 为期望值最高的 incumbent，认知层仍写“pivot 效应未定”；不得因峰值 859
把 baseline 切回 config A。

## 0. 历史背景（08-13 21:5x 快照，仅用于对照，禁止按其 last-2 执行）

- 探针 55468450：**已攒 41 局**（>35 接力线 → 8/15 判读局数大概率 45-60，可如期判读）；轨迹首样本 21:43 23-18 WR 0.561（**瞬读**，仅背景）。
- 瞬读分（仅背景，**不记账**）：探针 679.0（21:0x lb_watch）/ config A v2(55451759) 609.3 / v1(55431232) 650.3。v1 已出 last-2 仍在打。
- last-2 = {探针 55468450, config A v2 55451759 锚}。
- 判读链路 08-13 晚已**预检 + A/B 补丁落地**：WR 主路径工具 `experiments/live_episodes_probe.py` ✓；轨迹采样 `experiments/probe_wr_watch.py` + interval Automation 每 4h 在跑（→ `experiments/runs/probe_wr_trajectory.jsonl`，脚本内 8/15 18:00 自灭闸）；8/14 20:17 预演 Automation 已挂（once）。**①-3 原两坑已补，直接用下面新用法。**
- **归因链路 08-13 22:4x 已全规模预跑**：41 局 replay 全量预拉（`live_replays_probe/`，8/15 只需增量拉新局），probe 源 diff 全跑通（`top_pilot_diff_probe.json`，40 局入组 + 1 镜像正确排除）。**预览观察（瞬读不记账）**：探针 vs configA 同仪器并排，核心换位/能量指标**无系统差**（t8vol 探针 L1.5/W1.59 vs configA L1.14/W1.89；nrgA% 略升）——与本地闸 −0.50pp、瞬读 WR 0.561 在噪声带内三者互洽。**口径警示**：方案文档里「我们付费 retreat 0.42/局」的历史数字与现行 type8 仪器读数（configA 1.14/1.89）对不上——8/15 归因**只用同仪器并排表**，勿引用历史单点。

## ① 当前活跃件 live 判读（主议程）

**判读规则（两层）**：行动层管理的是“明天哪个候选成为最新提交”，并不存在平台侧
自动保留 live 前两名；认知层只有 WR **>0.62** 才可宣称可识别正效应。
`0.44–0.62` 写作“功效不足/未定”。σ 量级：n≈42-72 下 σ≈6-8pp，当前新件
更只有个位数局，点估计只用于监控，不能据此追单。

执行步：

1. **先发现活跃集**：`kaggle competitions team-submissions 16640688 --format json`；
   禁止从 HANDOFF 记忆 ref。再下载/读取完整 leaderboard 本队行，确认最新件确实关联团队榜。
2. **WR 主路径**：对活跃集两个 ref 分别显式运行 `experiments/live_episodes_probe.py
   --ref <REF> --out <独立JSON>`，stdout 直出 n/W/L/WR。禁止把两件写进同一 JSON。
3. **（仅 WR>0.62 需要归因时）拉 replay 做行为归因——08-13 两坑已补（A 补丁 A2/A3），用法如下**：
   - 拉取：`/opt/homebrew/bin/python3 experiments/live_loss_pull.py --rows experiments/runs/live_episodes_probe.json --out experiments/runs/live_replays_probe --include-wins`（5s 限速，~40 局约 4 分钟；**勿混入 `live_replays/`**：ours 源按目录全量扫，混入会把 config A 42 局与探针局合并污染）；
   - 分析：`/opt/homebrew/bin/python3 experiments/pilot_investigation/top_pilot_analyze.py --source probe --stage diff` → 产物 `experiments/runs/top_pilot_diff_probe.json`（**独立文件，不覆盖** config A 归档件）；对照锚 `experiments/pilot_investigation/artifacts/top_pilot_diff.json`（config A 六维：付费 retreat vol 0.42/局等）；
   - **我方 TeamName 现在是「可抽奖的冰棒」**（08-13 冒烟实证：显示名已改，Daniel1547 是 config A 时代的名；解析器已内建双名兼容，无需手改）。
4. 判读口径背景：链 cheatsheet ⑤（命令）⑥（残差三腿/live 配比）；σ 铁律链 `retreat_probe_plan.md`。
5. 落账：ledger 记“活跃集 + 最新计分件 + 两件各自局数/WR”，**不占 #135**。

## ② dying_674 翻牌三条件（全满足才动）

旧的 dying_674 三条件翻牌仅适用于“没有更强独立候选”的回滚情形。当前优先顺序是
跨策略候选（`scripts/candidate_delivery.py`）；只有它们均失败或出错，且额度仍有余，
才考虑 flag 翻牌，并必须完成净环境解包/两座位真执行后提交。不得让自动化以旧 prompt
单方面触发 `FLAG_DYING_674=True`。

## ③ 收官指针（8/15 不执行）

收官记账只走 `reports/2026-08-17_收官checklist.md`（执行窗口 8/17 晚 / 8/18 晨，结算稳定后）。观测哨 `f6bd5dac` **留到 final log 之后才 disable**；周报 `530a9aba`（8/19 09:20 自灭）、收官提醒 `9a7c7fda`（8/17 20:00）不动。
新增两件（08-13 B 补丁；08-14 已按新口径调整）：轨迹采样 `automation_512d20d3`（每 4h；**自灭闸已延至 8/17 07:30**，默认 ref 已随工具改为跟随最新活跃件，轨迹行带 `ref` 字段）——**8/17 冻结后顺手 disable**；8/14 晚 `automation_3d99de89`（once 20:17，**08-14 12:2x 已重写为「活跃集盘点记分牌」**，产物 `reports/20260814_记分牌.md` 可作本会输入参考）。

## ④ 冻结清单（全程）

- `submission_baseline/` 一个字节不动；恢复提交复用了盘上已存在的精确 tar，未改源码。
- 08-14 已因机制纠错完成恢复，**今天不再提交**。08-15/08-16 每天默认一发主候选；
  第 2 发只允许 ERROR 修复或明确恢复，不允许根据个位数局瞬时分追涨杀跌。
- config A 镜像 / arena 资产 / runbook（`experiments/bc_fidelity/NEXT_CAMPAIGN_RUNBOOK.md`）只读可用——**全部是下一场件，本会不执行**。
- 四件自动化原样；本件执行完即完，不衍生新议程。
