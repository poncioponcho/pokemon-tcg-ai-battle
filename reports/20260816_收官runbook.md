# 2026-08-16 收官 runbook

依据：`reports/20260815_决赛机制核查_kimi.md` 的网页端实证。官方自动取 latest-2，
无需手动勾选；2026-08-17 07:59 CST 截止后仍继续约两周 episodes 才定榜。

## 唯一目标

在截止锁定时 latest-2 均为同一个冻结的 exact Grim v22。8/16 只提交一发；不得提交
RL、Router、retreat、测试包或任何重新打包件。今晚 8/15 不再提交。

冻结件：

- archive：`artifacts/grim_v22_final/submission.tar.gz`
- archive SHA：`599e19ae9c6f5f09160565a9ffb0f3662062bde921dd196ad20c81f6b4c8bfcf`
- manifest：`artifacts/grim_v22_final/submission.tar.gz.manifest.json`
- main SHA：`d80d33c570ba5dff445f3be60bcbd038598bd418eeb44c1120f3fbea893cba98`
- deck SHA：`92b92bac9f9163ecff933b3dc39294d2cc154c8684f3c8497877661419ebc59d`

## 07:55–08:05 CST：只读预检

1. 等 Kaggle UTC 日额度约 08:00 CST 刷新；不要在刷新前误用 8/15 额度。
2. 执行 SHA 校验，输出必须逐字匹配上方 archive SHA：

   `shasum -a 256 artifacts/grim_v22_final/submission.tar.gz`

3. 核验 manifest 的 `status=PACKED`、`validation.status=PASS`、60 张牌、60 个成员：

   `jq '{archive_sha256,member_count,status,validation_status:.validation.status,deck_count:.validation.deck_count}' artifacts/grim_v22_final/submission.tar.gz.manifest.json`

4. 只读盘点当前 team submissions，确认 latest-2 仍为 refs `55516725`、`55499962`
   且二者均来自相同 v22 归档。若出现任何未知有效提交，停止并先查来源。

## 08:05–09:00 CST：唯一一次提交

早交的目的只是留足 ERROR 重试窗口，不是抢短期分数。命令：

`/opt/homebrew/bin/python3 -B scripts/candidate_delivery.py submit --archive artifacts/grim_v22_final/submission.tar.gz --manifest artifacts/grim_v22_final/submission.tar.gz.manifest.json --description 'grim_v22_final exact final-slot refresh | archive 599e19ae9c6f' --competition pokemon-tcg-ai-battle --record reports/candidate_delivery.jsonl`

保存返回的整数 ref。成功后立即停止所有提交；latest-2 应变为
`{新 ref exact-v22, 55516725 exact-v22}`，旧 ref `55499962` 出槽无风险。

## 状态与观测时间线

- 立即：用返回 ref 执行
  `/opt/homebrew/bin/python3 -B scripts/candidate_delivery.py inspect --ref <NEW_REF>`；
  记录 UPLOADED/PENDING、ref、时间、五个冻结 SHA 到 ledger。
- +5 分钟：复查 submission 状态和 latest-2。目标是 COMPLETE/有效；分数不作 GO/NO-GO。
- +15 分钟：再次确认 latest-2 均为 exact-v22，并保存 team-submissions/leaderboard 快照。
- +60 分钟：抓取一次 PUBLIC episodes 与分数快照，只用于审计初始化轨迹，不改提交决策。

若状态为 ERROR：先 inspect 错误；只允许用**同一 archive SHA**重试。不得为了修复而现场重包。
若 COMPLETE：无论短期分数是 500、600 或 800，都禁止第二发。

## 截止前封口

- 8/16 白天：只读观察，不再提交。
- 8/17 07:15 CST：最终盘点 latest-2、两 ref 状态、团队榜和候选交付日志。
- 8/17 07:30 CST：完成 `reports/2026-08-17_收官checklist.md`；保留29分钟缓冲。
- 页面没有 final-selection 控件，不执行手动勾选。唯一异常分支是 latest-2 中出现非 v22
  或新 ref ERROR；否则绝不在截止前追加一发。

## 截止后约两周

每天一次只读快照：两 ref 的 score、PUBLIC n/W/L、对手均分、对手强度校正值和团队 rank。
明确标注 submission ref，禁止把两件的分数拼成单一时间序列。决赛期不可提交，监测只为
最终复盘和赛后 matchup-router/R1 实验设计。

## 硬停止条件

- archive SHA 或 manifest 任一字段不符；
- 当前 latest-2 出现来源不明的有效件；
- Kaggle 截止/额度页面与 2026-08-15 网页核查不一致；
- submit 返回值缺少 ref，或 inspect 无法确认状态。

遇到上述任一项不猜测、不改包，保留两个现有 v22 槽位并升级人工复核。
