Key findings:
- Git HEAD is e077b61 ("v23.2"), but `main.py` is **modified** (uncommitted) — it has the NN code (`_NN_MODE='rerank'`) which is NOT in the last commit. So the NN was added after e077b61, uncommitted.
- **No `.npz` file exists anywhere** in the repo.
- `submission/main.py` differs from `main.py` — it's the older non-NN version.
- grep for `_NN_MODE` in `submission/main.py` returned nothing → **the packaged/uploaded `submission/main.py` does NOT contain the NN at all.**

So: the `submission/` folder is the stale (pre-NN) version, while the live `main.py` has the NN. Which one got uploaded? The submission description mentions the NN (val_top1=0.5379...), so the NN version likely went up — but through `pack.sh` which packages the **current main.py** (NN version) + deck.csv + npz-if-present. Since there's no npz, the NN silently degrades to pure rules at runtime.

Let me verify the submission description: "4dce430 best single-head pure-python NN (val_top1=0.5379 winrate=0.6763@800) no-". The commit hash `4dce430` is **not** in the git log (HEAD is e077b61). So the submitted code was committed on a detached/other state and the `.npz` — the actual trained NN weights — probably wasn't committed (untracked, then cleaned) → **the tar went up WITHOUT model_student.npz**, so NN never actually engaged.

This is a very coherent diagnosis: the submission claims NN but the weights file wasn't packaged, so real behavior = pure rules (i.e. essentially the same c291 rules that already worked). That alone doesn't "fail validation" though.

For "Validation Episode failed", I need the actual invalid-action mechanism. The NN bug I identified — `_nn_consult` returns `nn_act` **without `_sanitize`**, violating length==maxCount when `max_count > n_opts`, and returns `list(range(n))` via the shortcut at line 2992 when `max_count > n` (also length mismatch) — is the concrete defect that would abort a validation episode. Also `_nn_consult` at line 2908: `rule_action[0] in (int(x) for x in order[:3])` — fine.

Also important: if NN actually loaded and ran (hypothetically), `_nn_build_features` line 2834 `(mine.get('active') or [None])[0]` — if `active` is a dict not a list, `[0]` takes the first dict key; but it's guarded by `.get('active')`, likely a list.

Given 300字 limit, I'll present the two most probable causes:

**Cause 1 (最可能): NN 模型文件缺失，提交的其实是"伪NN"纯规则版，且代码路径上存在未过 `_sanitize` 的动作。** `model_student.npz` 全仓库不存在，pack.sh 只在文件存在时才打入（pack.sh L36-39），提交描述里的 `val_top1=0.5379` 是开发机上的蒸馏模型，未随包上传 → Kaggle 上 `_nn_lazy_load` 恒 False，退回纯规则。这个版本与通过验证的旧版本行为不同，且 `_nn_consult` 返回的 `nn_act` 未过 `_sanitize`，当 `max_count > n_opts` 时动作长度 < maxCount，违反"长度==maxCount"铁律 → 校验局中止。

**Cause 2:** `agent()` 的快速路径 line 2992 `max_count >= n: return list(range(n))` —— 返回长度=min 而非 maxCount，同样可能长度不匹配。

Fix directions: 将 npz 纳入打包（git 跟踪 + pack.sh 校验必备）；对 NN 输出也跑 `_sanitize`；把快速路径改为 `_sanitize(list(range(min(max_count,n))), n, max_count)`；或直接把 `_NN_MODE='off'` 保持稳定提交。

Let me write the diagnosis concisely in Chinese Markdown under 300 chars. Given it's a terminal deliverable, I'll keep it tight.

Actually I should double check the length-mismatch claim at line 2992. max_count>=n → returns range(n), length = n. If max_count > n then length n < max_count. That's a mismatch but it's the pre-existing behavior. Fine to mention as secondary.

Let me finalize under 300字.诊断完成。核心证据：全仓库无 `model_student.npz`，git HEAD(e077b61) 不含 NN 代码，`main.py` 为未提交改动，`submission/main.py` 仍是旧版无 NN。

## PTCG 校验局失败诊断

### 1. 最可能原因

**(A) NN 权重没被打包，"NN 提交"实为空壳，且 NN 路径动作未过合法性校验。**
`model_student.npz` 全仓库不存在；`pack.sh`(L36-39) 仅在文件存在时才打入包。提交描述声称"best single-head pure-python NN"(commit `4dce430` 未在 git 历史)，但该权重文件未随 tar 上传 → 线上 `_nn_lazy_load()`(main.py L2728) 永返回 False，静默退回纯规则——与你之前通过验证的行为不同，属不可复现的"伪 NN"。

**(B) NN 咨询路径漏 `_sanitize`，破坏动作铁律。**
`_nn_consult`(L2889-2915) 直接 `return nn_act[:max_count]`，未走 `_sanitize`；当 `max_count > n_opts` 时返回长度 < maxCount。`agent()` 快速路径 L2992 `max_count>=n: return list(range(n))` 同理可能长度= n ≠ maxCount。长度不匹配即非法动作 → **Validation Episode failed** 直接中止。

### 2. 排查/修复要点

- 把 `model_student.npz` 提交进 git 并确保 `pack.sh` 校验"缺 npz 就报错"（而非静默降级），`pack.sh` L35-40。
- main.py L2902-2914：NN 输出统一 `return _sanitize(nn_act, n_opts, max_count)`；L2992 改为 `_sanitize(list(range(min(max_count,n))), n, max_count)`。
- 对齐 `submission/` 与 `main.py`（当前二者 diff 不一致），避免打成旧版。

### 3. 行动建议
先 `git add` 并确认 npz 随包上传、NN 输出过 `_sanitize`，否则本次"NN 提交"退化规则还可能触发非法动作失败。
